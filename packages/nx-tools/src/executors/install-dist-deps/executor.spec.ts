import { InstallFromDipsSchema } from './schema';
import executor from './executor';

const options: InstallFromDipsSchema = {};

// describe('Tarball packager Executor', () => {
//   it('can run', async () => {
//     const output = await executor(options);
//     expect(output.success).toBe(true);
//   });
// });